<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!--
    BendBSN App Icon
    Dark navy bg + white shield + teal cap band + blue medical cross
    Works as both "any" and "maskable" PWA icon (content in 80% safe zone)
  -->

  <!-- Background — solid fill, no gradient, works for maskable -->
  <rect width="512" height="512" fill="#1a3a5c"/>

  <!-- Teal nurse cap accent (sits above shield top) -->
  <rect x="148" y="102" width="216" height="38" rx="10" fill="#3dbdae"/>

  <!-- White shield body -->
  <path d="M 148,132 L 364,132 L 364,306 Q 364,410 256,444 Q 148,410 148,306 Z"
        fill="white"/>

  <!-- Blue medical cross (centered in shield) -->
  <!-- Vertical bar -->
  <rect x="234" y="162" width="44" height="214" rx="8" fill="#3a74e8"/>
  <!-- Horizontal bar -->
  <rect x="178" y="222" width="156" height="44" rx="8" fill="#3a74e8"/>
</svg>
