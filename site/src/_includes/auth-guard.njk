    <!-- Auth Guard - Redirect to login if not authenticated -->
    <script nonce="{{ nonce }}">
        (function() {
            // Quick check - if no session data at all, redirect immediately
            if (!sessionStorage.getItem('bsn9b_user') && !localStorage.getItem('bsn9b_user')) {
                window.location.href = '/';
            }
        })();
    </script>
